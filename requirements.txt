pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
streamlit>=1.32
plotly>=5.18
anthropic>=0.34   # optional: only used if ANTHROPIC_API_KEY is set (agent LLM mode)
razorpay>=1.4      # optional: only used for the live-data path (data/razorpay_client.py)
pytest>=7.4
